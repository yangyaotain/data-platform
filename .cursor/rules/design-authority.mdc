---
description: 数据中台原型 — 文案以用户截图为准，视觉以当前设计体系为准
alwaysApply: true
---

# 设计与内容原则（数据中台 V4.0 原型）

## 文案与功能范围

- **唯一依据**：页面文案、示例数据、交互说明以用户提供的 **截图** 与 **文字说明** 为准。
- **禁止擅自发挥**：用户未明确要求时，不扩写、不改写、不增补业务文案或示例（含历史记录、提示语、表格样例等）。

## 组件与样式

- **以当前实现为基准**：沿用现有组件结构（布局、类名、模块化 CSS/JS 组织方式）。
- **风格一致**：颜色、圆角、间距、字体层级与现有 `variables.css`、`layout.css`、`components.css` 及 `css/pages/*` 保持一致；新增页面/模块应复用同类组件样式，避免另起一套视觉语言。

## 冲突时的处理

- 截图/说明与旧代码不一致 → **以最新截图与说明为准** 改文案或结构。
- 截图未规定而需补全（如间距）→ 在 **当前设计体系内** 做最小补全，不引入新主题。
