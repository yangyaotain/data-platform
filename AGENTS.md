# 数据治理平台原型约定

本项目是数据治理/数据资产管理类静态原型，采用 `index.html` + `css/` + `js/` 的模块化页面结构。所有新增和优化必须贴合现有数据治理平台风格，不另起视觉体系。

项目结构：
- `index.html`：单页原型入口。
- `css/variables.css`、`css/components.css`、`css/layout.css`：全局变量、组件和布局。
- `css/pages/`：页面级样式。
- `js/config.js`、`js/sidebar.js`、`js/router.js`、`js/app.js`：配置、导航、路由和应用入口。
- `js/pages/`：页面级逻辑。
- `js/components/`：复用组件。

原型修改规则：
- 先定位同类页面再修改，例如数据源目录树、元数据搜索、质量报告、稽查任务、数据地图等已有实现。
- 用户说“参考某页面格式”时，优先复用该页面的信息结构、表格列、卡片和交互模式，不理解为合并两个页面功能。
- 数据质量、稽查报告、元数据、数据地图等示例数据必须符合数据资产管理语境；统计数值要自洽，避免通过率、问题记录数、总记录数互相矛盾。
- 导出、下载等原型交互优先做可演示的前端即时生成或提示，不提前生成大量无关文件。
- 不启动服务、不打开浏览器测试，除非用户明确要求。

公共日期组件（2026-09-04 确认）：
- 统一使用 `js/components/date-picker.js` 和 `css/components/date-picker.css`，入口在 `index.html` 公共加载。页面不得再独立实现日历或用两个原生日期框拼接时间范围。
- `DP.datePicker.render(options)` 返回组件 HTML，组件使用全局事件委托，页面重绘后无需重复初始化。
- 单个日期使用 `mode: 'single'`，点击展开单日历，选中日期立即回填；日期范围使用 `mode: 'range'`，一个输入框展开左右双日历，支持跨月、跨年、同日及反向选择，范围高亮。
- 双日历选中起点时不通知查询，选完终点后一次性回填起止值、收起浮层，只触发一次冒泡 `change`；清除同样立即触发。取消未完成选择（点击外部、Esc、离开控件）保留原值，不触发查询。不增加确定、取消或独立清除时间按钮。
- 查询页面的日期完成选择或清除后立即查询，并同时使用文本框当前关键词；文本输入仍只在查询按钮或 Enter 后查询。
- 默认按日期选择，显示 `YYYY-MM-DD`。日志等按天筛选的页面用 `output: 'datetime'`，起点转为 `00:00:00`，终点转为 `23:59:59`，结果表中原有精确时间不变。
- 单次执行等业务需要精确时间时，用 `mode: 'single', output: 'datetime', withTime: true`，同一控件内保留时分秒。周期调度的纯时间、分钟、星期等配置保持原有业务含义，不当作日期范围。
- 页面通过 `startAttrs` / `endAttrs` 或单值的 `valueAttrs` 配置隐藏输入属性；组合值表单可用 `combinedAttrs`。收到一次 `change` 时必须同时读取完整起止值，不能只更新变化的一端。组件不负责保存表单或提交业务数据。
- 复用项目字体和主题变量：查询控件高 32px、主要文字 13px、辅助说明 12px、圆角 4px；浮层放在页面滚动容器外，自动避让窗口边缘。页面切换后关闭浮层，不残留旧日历。

提交和检查：
- 用户要求提交/部署时使用全局 `git-deploy-update` 技能。
- 默认运行 `git diff --check`。
- 修改 JS 后做轻量语法检查；系统 `node` 不可用时使用 Codex bundled Node。
- 中文显示异常时先用 UTF-8 重读确认。
